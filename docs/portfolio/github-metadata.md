# GitHub Metadata · Prepared, Not Applied

## Repository Description

AI Medical Device Sales Copilot — an evidence-driven portfolio prototype for customer understanding, visit preparation, roleplay, opportunity assessment, and versioned sales review.

Short alternative: Evidence-driven AI sales copilot prototype for medical device sales workflows.

## Recommended Topics

`ai`, `sales`, `sales-copilot`, `medical-devices`, `medical-device-sales`, `sales-intelligence`, `human-in-the-loop`, `evidence-driven`, `javascript`, `nodejs`, `prototype`, `portfolio-project`.

Prioritize `ai`, `sales-copilot`, `medical-devices`, `sales-intelligence`, `human-in-the-loop`, `javascript`, `nodejs`, `prototype` if fewer are desired. The AI topic refers to workflow design/Mock, not an implemented external model integration.

## Git Preparation

Git repository not initialized. NO REMOTE CONFIGURED. No branch, commit or tag exists in the current directory. GitHub CLI is not available in the inspected environment; no authentication attempt was made.

After the owner resolves publication holds, initialize a repository, review an explicit file list and create the first commit. Suggested commit message:

```text
docs: prepare MedSales AI v1.1.0 GitHub release
```

Only after a reviewed commit and a clean worktree, the proposed annotated tag command is:

```sh
git tag -a v1.1.0 -m "MedSales AI V1.1.0 Portfolio Release"
```

This command has not been executed. Do not tag uncommitted documentation or overwrite an existing tag. Remote creation and push require a later explicit user instruction.
